{
  "name": "queue-proxy-lambda",
  "version": "1.0.0",
  "description": "Lambda proxy for SQS paper queues",
  "type": "module",
  "main": "index.js",
  "scripts": {
    "test-build": "npm run-script test && npm run-script build",
    "build": "npm prune --production && rm -f ./function.zip && zip -r ./function.zip . -x '*.md' -x '*.env'",
    "test": "mocha index.test.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@aws-sdk/client-sqs": "^3.882.0"
  },
  "devDependencies": {
    "chai": "^6.0.1",
    "mocha": "^11.7.2",
    "sinon": "^21.0.0"
  }
}
